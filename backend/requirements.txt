# Cảng cá Lộc — Django core (Level 4)
# Phiên bản ghim theo quyết định stack: Django 100% lõi, PostgreSQL.
Django>=5.0,<5.2
djangorestframework>=3.15
psycopg[binary]>=3.1
python-dotenv>=1.0
dj-database-url>=2.1
celery>=5.3
redis>=5.0
# Production serving (Cloud Run)
gunicorn>=22.0
whitenoise>=6.6
django-cors-headers>=4.4
