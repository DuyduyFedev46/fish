# URL gốc của Django API (Shop API công khai). Mặc định dùng khi không set: http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Đặt = 1 để Shop dùng dữ liệu mock (khi backend chưa chạy). Đặt = 0 hoặc bỏ trống để gọi API thật.
NEXT_PUBLIC_USE_MOCK=1
